# === Rust ===
target/
**/*.rs.bk
*.pdb
*.rlib
*.rmeta

# === Cargo ===
.cargo/
# Keep Cargo.lock committed for the application binary workspace.

# === macOS ===
.DS_Store
.AppleDouble
.LSOverride
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
__MACOSX/

# === Windows ===
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# === Linux ===
*~
.directory

# === JetBrains (RustRover, IntelliJ, CLion) ===
.idea/
*.iml
*.iws
*.ipr
out/

# === VSCode / Cursor ===
.vscode/
.cursor/*
# Keep the tracked agent rules while ignoring the rest of .cursor/
!.cursor/rules/
!.cursor/rules/*
*.code-workspace
.history/

# === Zed ===
.zed/

# === Neovim / Vim ===
*.swp
*.swo
*~
.*.swp
.*.swo
.nvim/
.vim/

# === Environment & Secrets ===
.env
.env.*
!.env.example
.env.local
.env.*.local
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
secrets/
credentials.toml
credentials
.credentials
!examples/demo-target/vulnerable-files/.env
# Extensionless private keys (demo target exception below)
id_rsa
id_ecdsa
id_ed25519
id_dsa
!examples/demo-target/vulnerable-files/id_rsa
.ssh/
# direnv may export secrets into the shell
.envrc
.direnv/

# === Database & Storage ===
*.db
*.sqlite
*.sqlite3
vest.db
# SQLite sidecar files from local scan runs
*.db-wal
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
# Local Vest workspace / state under the repo (do not use ~ expansion; gitignore is literal)
.vest/

# === Logs ===
*.log
logs/
nohup.out

# === Temporary & Backup ===
*.tmp
*.temp
*.bak
*.backup
*.old
*.orig
*.swp

# === Planning / local agent artifacts ===
.plan/
.claude/
.codex/
.aider*
.agent/
.opencode/

# === Config Overrides ===
vest.toml.local
vest.toml.user
config.local.toml
*.local.json
# opencode config may hold provider keys / MCP tokens
opencode.json
opencode.jsonc

# === Docker ===
.docker/
docker-compose.override.yml

# === External Tools ===
nuclei-templates/
frida-scripts/
# Local installs under tools/ — keep vest-tools crate source
/tools/

# === Test / Benchmark Artifacts ===
test-results/
coverage/
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
lcov.info
examples/.vest-demo/
examples/reports/generated/
examples/demo-target/vulnerable-binary/vuln
# Flask private data dir for the demo webapp (local run state)
examples/demo-target/webapp/instance/
*.profraw
*.profdata
flamegraph.svg
perf.data
perf.data.old
.tar.gz
.zip
*.dmp
core
core.*

# === Generated reports at repo root ===
/report.json
/report.md
/report.html
/report.sarif
*.sarif

# === Package Managers ===
node_modules/
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# === CI/CD ===
.github/workflows/*.local.yml

# === Agent / AI local state ===
AGENTS.local.md
.canvas/

# === Misc local overrides ===
*.local.toml
